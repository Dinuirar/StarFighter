#ifndef COMMON_H
#define COMMON_H

enum shipClass {
    INTERCEPTOR, BERSERK, TANK
};

enum attackType {
    LASER, PLASMA, KINETIC
};

enum difficulty {
    EASY, MEDIUM, HARD
};

enum gameMode {
    ONE_PLAYER, TWO_PLAYERS
};

enum aiAlgorithm {
    SIMPLE, ADVANCED
};

#endif // COMMON_H
